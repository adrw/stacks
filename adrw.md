# Deploying

```
sudo VOLUME_PATH=/volume1/docker/nextcloud/instance/ DOMAIN=cloud.adrw.xyz STACK_NAME=nextcloud MYSQL_ROOT_PASSWORD=myp@ssw0rd docker stack deploy -c nextcloud-onlyoffice.yml nextcloud
```
